datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model farmacia{
  id    Int    @id @default(autoincrement())
  fabricacao String 

  nome_pessoa String
  nome_remedio String

  funcionario String
}
